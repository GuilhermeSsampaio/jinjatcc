import React from 'react';

export default function {{ page.component }}() {
  return (
    <div>
      <h2>{{ page.title or page.component }}</h2>
      <p>Página gerada automaticamente.</p>
    </div>
  );
}